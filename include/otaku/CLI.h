#pragma once
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int CheckCLI(char** args)
{
    if (strcmp(args[0], "--verbose") == 0, strcmp(args[0], "-v") == 0)
    {
        
    }
}